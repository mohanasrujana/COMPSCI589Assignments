If running locally, create a conda environment or python virtual environment. Install pip and then run:

pip install -r requirements.txt 

If running on Google Colab, all needed packages are already installed.